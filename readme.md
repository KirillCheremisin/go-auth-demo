Аутентификация и авторизация способами session-based, token-based.

Эндпоинты:
    GET  /health
    POST /register
    POST /login
    POST /login-jwt
    GET  /session/profile (protected)
    POST /session/logout (protected)
    GET  /session/users (protected)
    GET  /jwt/profile (protected)